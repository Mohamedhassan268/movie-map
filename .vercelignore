# The live app (index.html, vendor/), the api/ function, and the docs/ catalog
# graph demo deploy. docs/ is served at /docs/ (loads docs/data.json).
# The offline Arabic-first pipeline scripts aren't served.
scraper/
ai_tagging/
similarity/
*.md
mood-tags-vocabulary.md
